# Copy this file to .env and fill in real values.

# Base URL of the SkinLab NestJS API
REACT_APP_API_URL=http://localhost:3000

# Port the CRA dev server runs on (Create React App reads this directly).
# Match server/.env's FRONTEND_URL so backend CORS/Google OAuth redirects work.
PORT=3001
